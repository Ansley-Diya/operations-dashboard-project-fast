# Dependencies — never commit node_modules
# npm install regenerates this from package.json
node_modules/

# package-lock.json is auto-generated by npm install
# It's safe to commit it (it locks exact versions) but
# some teams exclude it — include or exclude based on your team's preference
# package-lock.json

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V7
.Trashes
Thumbs.db

# Editor directories and files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build output (if you ever add a build step)
dist/
build/
